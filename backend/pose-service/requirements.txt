# Core ML Libraries
torch==2.0.1
torchvision==0.15.2
torchaudio==2.0.2
numpy==1.26.0
opencv-python==4.8.0.76

# CRITICAL: joblib must be installed FIRST before PHALP
# PHALP imports joblib during its own initialization
joblib>=1.0.0

# PyTorch3D (critical for 4D-Humans mesh operations)
# Installed from source in Dockerfile due to CUDA compatibility

# Mesh and Rendering
trimesh==3.20.2
pyrender==0.1.45

# Configuration and Utilities
omegaconf==2.3.0
hydra-core==1.3.0
scikit-image==0.21.0
scipy==1.11.0
tqdm==4.66.0
requests==2.31.0
pyyaml==6.0

# 4D-Humans Dependencies (exact versions from setup.py)
gdown==4.7.1
pytorch-lightning==2.0.0
smplx==0.1.28
einops==0.7.0
timm==0.9.0
webdataset==0.2.48
dill==0.3.7
pandas==2.0.0
chumpy @ git+https://github.com/mattloper/chumpy

# PHALP Dependencies (temporal tracking for video-level pose estimation)
yacs==0.1.8
phalp==0.1.3

# Detectron2 (person detection for HMR2 - includes ViTDet models)
# Installed from source in Dockerfile for Python 3.9 compatibility
# detectron2 @ git+https://github.com/facebookresearch/detectron2

# Flask API
flask==3.0.0
flask-cors==4.0.0

# Additional utilities
Pillow==10.0.0
matplotlib==3.8.0
